import { StyledFooter } from "./styles";

const Footer: React.FC = () => {
    return (
        <StyledFooter>
            <a>ЛОГОТИП</a>

            <p>BlinovDev</p>
        </StyledFooter>
    );
}

export default Footer;